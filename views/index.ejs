<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>vChess Multiplayer</title>
  <link rel="stylesheet" href="/index.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    ::-webkit-scrollbar {
      width: 8px;
    }
    ::-webkit-scrollbar-track {
      background: #f0f4ff;
    }
    ::-webkit-scrollbar-thumb {
      background: #a5b4fc;
      border-radius: 4px;
    }
  </style>
</head>
<body class="bg-gradient-to-br from-[#fdfbff] to-[#f0f4ff] min-h-screen p-4 font-sans text-gray-800">

  <div class="fixed inset-0 bg-white -z-10"></div>

  <div class="absolute top-4 left-1/2 -translate-x-1/2 bg-white px-6 py-2 rounded-full shadow-md text-gray-800 font-medium z-20 text-center">
    🧑 You are: <span class="text-indigo-600 font-bold"><%= username %></span>
  </div>
  <input type="hidden" id="player-username" value="<%= username %>" />

  <div id="loader-overlay" class="fixed inset-0 bg-black bg-opacity-70 flex flex-col items-center justify-center z-40">
    <h2 id="loader-message" class="text-3xl font-extrabold text-white mb-6 animate-pulse">Finding an opponent...</h2>
    <div class="h-12 w-12 border-4 border-white border-t-transparent rounded-full animate-spin"></div>
  </div>

  <div id="popup-container" class="fixed inset-0 z-50 pointer-events-none"></div>

  <div class="main-layout flex flex-col md:flex-row items-center md:items-start justify-center w-full max-w-screen-xl mx-auto gap-6 md:gap-10 mt-24 z-10">

   
    <div id="move-history" class="hidden md:flex flex-col w-72 h-[600px] bg-[#fff7ed] border border-yellow-300 rounded-xl shadow">
      <div class="bg-yellow-200 text-yellow-900 font-semibold px-5 py-3 text-lg rounded-t-xl flex items-center gap-2">
        ⏳ Move History
      </div>
      <ol id="move-list" class="list-decimal list-inside text-sm p-4 space-y-1 overflow-y-auto text-brown-600 font-mono"></ol>
    </div>

  
    <div id="game-container" class="flex flex-col items-center gap-4 transition-transform duration-500">
      <div id="black-info" class="bg-white rounded-xl border shadow p-4 w-72 text-center">
        <h3 class="font-semibold text-lg text-gray-700">Black: <span id="black-name" class="text-black">Waiting...</span></h3>
        <div class="mt-1 text-lg text-black font-mono">05:00</div>
      </div>

      <div class="chessboard-container w-[90vw] max-w-[600px] aspect-square bg-[#fdf6e3] border-4 border-gray-400 rounded-lg shadow-xl overflow-hidden">
        <div class="chessboard w-full h-full"></div>
      </div>

      <div id="white-info" class="bg-white rounded-xl border shadow p-4 w-72 text-center">
        <h3 class="font-semibold text-lg text-gray-700">White: <span id="white-name" class="text-black">Waiting...</span></h3>
        <div class="mt-1 text-lg text-black font-mono">05:00</div>
      </div>
    </div>


    <div id="chat-container" class="hidden md:flex flex-col w-72 h-[600px] bg-[#e6fffa] border border-teal-300 rounded-xl shadow">
      <div class="bg-teal-200 text-teal-900 font-semibold px-5 py-3 text-lg rounded-t-xl flex items-center gap-2">
        💬 Chat
      </div>
      <div id="chat-messages" class="flex-1 p-4 overflow-y-auto text-sm text-gray-800 space-y-2 font-mono"></div>
      <div class="flex border-t border-gray-200">
        <input id="chat-input" type="text" placeholder="Type a message..." class="flex-1 px-4 py-3 text-gray-700 bg-teal-50 outline-none rounded-bl-xl" />
        <button id="send-chat" class="bg-teal-600 hover:bg-teal-700 text-white px-6 rounded-br-xl font-semibold">Send</button>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/chess.js@0.10.3/chess.min.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script type="module" src="/Javscripts/index.js"></script>
</body>
</html> -->


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>vChess Multiplayer</title>
  <link rel="stylesheet" href="/index.css" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }

    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: #f0f4ff;
    }

    ::-webkit-scrollbar-thumb {
      background: #a5b4fc;
      border-radius: 4px;
    }

    #ai-suggestion-modal {
      background-color: rgba(0, 0, 0, 0.5);
      display: none;
    }

    .modal-content {
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from { transform: translateY(-50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>

<body class="bg-gradient-to-br from-[#fdfbff] to-[#f0f4ff] min-h-screen p-4 text-gray-800">
  <div class="fixed inset-0 bg-white -z-10"></div>

  <div class="absolute top-4 left-4 bg-white px-5 py-2 rounded-full shadow-lg text-gray-800 font-semibold z-20">
    <span class="text-gray-500">👤 You are:</span>
    <span class="text-indigo-600 font-bold tracking-wide"><%= username %></span>
  </div>
  <input type="hidden" id="player-username" value="<%= username %>" />

  <div id="loader-overlay" class="fixed inset-0 bg-gray-900 bg-opacity-80 flex flex-col items-center justify-center z-40">
    <h2 id="loader-message" class="text-3xl font-extrabold text-white mb-6 animate-pulse capitalize">Finding an opponent...</h2>
    <div class="h-12 w-12 border-4 border-white border-t-transparent rounded-full animate-spin"></div>
  </div>

  <div id="popup-container" class="fixed inset-0 z-50 pointer-events-none"></div>

  <div class="main-layout flex flex-col md:flex-row items-center md:items-start justify-center w-full max-w-screen-2xl mx-auto gap-y-8 md:gap-x-20 mt-20 z-10">
    
    
    <div id="move-history" class="hidden md:flex bg-white rounded-xl shadow-lg w-80 h-[600px] flex-col">
      <div class="bg-yellow-100 text-yellow-800 font-semibold text-lg px-6 py-3 rounded-t-xl tracking-wide border-b border-yellow-200 capitalize">
        ⏳ Move History
      </div>
      <ol id="move-list" class="list-decimal list-inside text-sm p-4 space-y-1 overflow-y-auto text-gray-700 font-mono"></ol>
    </div>

   
    <div id="game-container" class="flex flex-col items-center gap-6 transition-transform duration-500">
      <div id="black-info" class="bg-white rounded-xl shadow-lg p-4 w-72 text-center">
        <h3 class="font-bold text-xl text-red-600 capitalize">Black: <span id="black-name" class="font-mono text-lg">Waiting...</span></h3>
        <div class="mt-2 text-gray-500 capitalize">Time: <span id="black-timer" class="font-mono text-xl">05:00</span></div>
      </div>

      <div class="chessboard-container w-[90vw] max-w-[600px] aspect-square rounded-xl shadow-lg bg-white overflow-hidden">
        <div class="chessboard w-full h-full rounded-xl overflow-hidden"></div>
      </div>

      <div id="white-info" class="bg-white rounded-xl shadow-lg p-4 w-72 text-center">
        <h3 class="font-bold text-xl text-blue-600 capitalize">White: <span id="white-name" class="font-mono text-lg">Waiting...</span></h3>
        <div class="mt-2 text-gray-500 capitalize">Time: <span id="white-timer" class="font-mono text-xl">05:00</span></div>
      </div>
    </div>

   
    <div id="chat-container" class="hidden md:flex bg-white rounded-xl shadow-lg w-80 h-[600px] flex-col">
      <div class="bg-cyan-100 text-cyan-800 font-semibold text-lg px-6 py-3 rounded-t-xl tracking-wide border-b border-cyan-200 capitalize">
        💬 Chat
      </div>
      <div id="chat-messages" class="flex-1 p-4 overflow-y-auto text-sm text-gray-700 font-sans space-y-2">
     
      </div>
      <div class="flex border-t border-gray-200">
        <input id="chat-input" type="text" placeholder="Type a message..." class="flex-1 bg-cyan-50 text-gray-800 px-4 py-3 outline-none focus:ring-2 focus:ring-cyan-300 transition-colors duration-200 rounded-bl-xl" />
        <button id="send-chat" class="bg-cyan-600 hover:bg-cyan-700 text-white px-6 font-semibold tracking-wider rounded-br-xl">
          Send
        </button>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/chess.js@0.10.3/chess.min.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script type="module" src="/Javscripts/chess/index.js"></script>
</body>
</html>
