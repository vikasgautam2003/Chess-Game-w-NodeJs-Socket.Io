<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Welcome Back - Bonzo Chess</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #121212; /* Dark charcoal background */
    }
    .btn-primary {
      background-color: #c0a062; /* Muted gold */
      color: #121212;
    }
    .btn-primary:hover {
      background-color: #d4b57a; /* Lighter gold on hover */
    }
    .btn-secondary {
      background-color: transparent;
      border: 2px solid #4f4f4f; /* Gray border */
      color: #e0e0e0;
    }
    .btn-secondary:hover {
      background-color: #2a2a2a;
      border-color: #6f6f6f;
    }
    .text-gold {
      color: #c0a062;
    }
  </style>
</head>
<body class="text-gray-200 min-h-screen flex flex-col">

  <!-- Navigation Bar -->
  <nav class="bg-black/30 shadow-md p-4 flex justify-between items-center backdrop-blur-sm sticky top-0 z-30 border-b border-gray-800">
    <div class="text-2xl lg:text-3xl font-bold tracking-wider">BONZO</div>
    <div class="text-gray-400 text-sm md:text-base">Hello, <span class="font-semibold text-gray-200"><%= username %></span></div>
    <div>
      <a href="/auth/logout" class="text-gray-400 hover:text-white font-semibold px-3 py-2 md:px-4 rounded hover:bg-gray-800/60 transition text-sm md:text-base">
        Logout
      </a>
    </div>
  </nav>

  <!-- Main Content with Video Background -->
  <main class="relative flex-grow flex flex-col justify-center items-center overflow-hidden">
    <!-- Video Background -->
    <div class="absolute top-0 left-0 w-full h-full z-0">
      <video class="w-full h-full object-cover" src="/Videos/chess.mp4" autoplay loop muted playsinline></video>
      <!-- Darker Overlay for a more cinematic feel -->
      <div class="absolute top-0 left-0 w-full h-full bg-black/75"></div>
    </div>

    <!-- Content Container -->
    <div class="relative z-10 flex flex-col justify-center items-center px-6 py-12 text-center">
      <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-4 lg:mb-6 text-white drop-shadow-md">
        Welcome Back, <span class="text-gold"><%= username %></span>!
      </h1>
      <p class="text-gray-300 text-base sm:text-lg lg:text-xl mb-10 lg:mb-12 max-w-xl lg:max-w-3xl font-light">
        The board is set. Your next move awaits.
      </p>
      <div class="flex flex-col sm:flex-row items-center space-y-4 sm:space-y-0 sm:space-x-6">
        <a href="/user/play" class="btn-primary font-bold py-3 px-8 lg:py-4 lg:px-12 rounded-full shadow-lg transition-transform hover:scale-105 text-base lg:text-xl">
          ♟️ Play Now
        </a>
        <a href="/profile" class="btn-secondary font-bold py-3 px-8 lg:py-4 lg:px-12 rounded-full shadow-lg transition-transform hover:scale-105 text-base lg:text-xl">
          👤 View Profile
        </a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="text-gray-500 text-center py-4 lg:py-6 text-sm z-10 bg-black/30 border-t border-gray-800">
    &copy; 2025 Bonzo. All Rights Reserved.
  </footer>
</body>
</html>
