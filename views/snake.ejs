

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen bg-gradient-to-br from-gray-900 via-green-900 to-black p-4 font-[Poppins]">

    <div class="flex flex-col items-center w-full max-w-md sm:max-w-lg lg:max-w-2xl bg-gray-800/40 backdrop-blur-md rounded-2xl shadow-lg p-6 border border-green-500/40 relative">
        <h1 class="text-green-400 text-3xl sm:text-4xl font-bold mb-4 text-center drop-shadow-lg">🐍 Snake Game</h1>
        <div class="w-full flex justify-center">
            <div class="p-2 bg-gray-900 rounded-xl border-4 border-green-500 shadow-xl">
                <canvas id="gameBoard" class="bg-black rounded-lg" style="max-width: 100%; height: auto;" width="600" height="600"></canvas>
            </div>
        </div>
        
        <div class="mt-4 text-white text-lg sm:text-xl text-center font-medium">
          Player: <span class="text-green-400 font-semibold"><%= username %></span>
        </div>
        <div class="mt-3 text-gray-400 text-sm italic text-center">Use arrow keys to move. Eat the food, avoid hitting yourself!</div>

        <div id="startModal" class="absolute inset-0 bg-black/70 flex flex-col items-center justify-center z-20">
            <div class="bg-gray-800 p-6 rounded-xl text-center shadow-xl border border-green-500 w-80">
                <h2 class="text-green-400 text-2xl mb-4">Start the game?</h2>
                <button id="startBtn" class="bg-green-500 hover:bg-green-600 text-black font-bold py-2 px-6 rounded-full">Start</button>
            </div>
        </div>

        <div id="gameOverModal" class="absolute inset-0 bg-black/70 hidden flex-col items-center justify-center z-20">
          <div class="bg-gray-800 p-6 rounded-xl text-center shadow-xl border border-red-500 w-80">
            <h2 class="text-red-400 text-2xl mb-4">Game Over</h2>
            <p class="text-white mb-4" id="finalScore">Score: 0</p>
            <a href="<%= isGuest ? '/guest/home' : '/user/home' %>" class="bg-yellow-500 hover:bg-yellow-600 text-black font-bold py-2 px-6 rounded-full">Return Home</a>
          </div>
        </div>

    </div>

    <!-- New popup JS first -->
    <script src="/javscripts/snake/snake-popup.js"></script>
    <!-- Snake game logic -->
    <script src="/javscripts/snake/snake.js"></script>
</body>
</html>
